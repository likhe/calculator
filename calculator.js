"use strict";

const ready = function() {
  document.querySelector("form").addEventListener("submit", function(e))
}


/*Quand l'utilisateur clique sur le bouton type submit
 document.addEventListener("DOMContentLoaded", function() {
            document.querySelector("math").addEventListener("click", function() {
              //
                let val1 = parseInt(document.getElementById('num1').value);
                let val2 = parseInt (document.getElementById('num2').value);
                let result = val1 + val2;
                console.log(result);
                document.getElementById('result').innerHTML = result;
            })
        });
*/
